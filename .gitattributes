# The mwrap gateway is machine-generated and carries its own marker in
# bindings/matlab/.gitattributes. include/treeweave_version.h is generated too,
# but it is gitignored rather than committed, so it needs no marker here.
